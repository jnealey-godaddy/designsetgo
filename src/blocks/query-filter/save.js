/**
 * Query Filter — save.
 *
 * This block is dynamically rendered server-side (render.php).
 * The save function intentionally returns null.
 *
 * @since 2.1.0
 */
export default function save() {
	return null;
}
