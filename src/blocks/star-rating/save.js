/**
 * Star Rating — save component.
 *
 * Dynamic block rendered server-side via render.php: bindings only resolve
 * there, so nothing static is stored.
 *
 * @return {null} Null because this is a dynamic block.
 */
export default function save() {
	return null;
}
