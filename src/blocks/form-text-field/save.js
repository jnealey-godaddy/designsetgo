/**
 * Form Text Field Block - Save Component
 *
 * Dynamic block rendered server-side via render.php. No static HTML is saved,
 * so the serialized block is a single self-closing comment and authored /
 * translated / pattern-substituted field text can never trip block validation.
 * Historical static markup is migrated by ./deprecated.js.
 *
 * @return {null} Null because this is a dynamic block.
 */
export default function save() {
	return null;
}
