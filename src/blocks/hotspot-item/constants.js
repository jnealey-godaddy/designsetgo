// Used by the parent child-toolbar clone path. A clone must seed a new id so
// its marker and tooltip do not duplicate the source item's ARIA wiring.
export const HOTSPOT_ITEM_DUPLICATE_OVERRIDES = { uniqueId: '' };
