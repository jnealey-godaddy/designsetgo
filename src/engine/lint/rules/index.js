/**
 * Registered lint rules, in application order.
 *
 * Each rule module exports `{ id, severity, check?(node, ctx), checkTree?(tree, ctx) }`
 * (see `src/engine/lint/index.js` for the exact contract). Empty for now —
 * Tasks 10 and 11 add the actual design-mistake rules here.
 */
export const rules = [];
